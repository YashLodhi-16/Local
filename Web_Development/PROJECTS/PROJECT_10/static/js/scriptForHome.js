// grabing postContainer
const posterContainer = document.getElementById('posterContainer');
// set posterContainer top margin to navbar offset height
posterContainer.style.marginTop = navbar.offsetHeight + "px";